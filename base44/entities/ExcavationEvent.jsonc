{
  "name": "ExcavationEvent",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional free-form description (max 1000 chars)"
    },
    "user_email": {
      "type": "string"
    },
    "pack_name": {
      "type": "string",
      "description": "Display name of the excavation event (e.g. 'Echo Drift Excavation')"
    },
    "origin": {
      "type": "string",
      "description": "Signal origin of the dig site (e.g. 'Drift 7-\u03a3', 'Monolith Sector 12')"
    },
    "lore_tag": {
      "type": "string",
      "description": "Narrative tag attached to all cards surfaced in this event"
    },
    "rarity_bias": {
      "type": "string",
      "enum": [
        "common",
        "rare",
        "epic",
        "legendary",
        "mythic"
      ],
      "default": "common",
      "description": "Minimum guaranteed rarity floor for the event"
    },
    "card_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Card IDs surfaced from this event"
    },
    "cards_surfaced_snapshot": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "default": []
    },
    "stability_drift": {
      "type": "number",
      "default": 0,
      "description": "How much this event nudged the global Reality Pressure"
    }
  },
  "required": [
    "pack_name",
    "origin"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}